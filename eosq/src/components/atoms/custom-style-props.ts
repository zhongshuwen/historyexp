import { system } from "styled-system"

export const whiteSpace = system({ whiteSpace: { property: "whiteSpace" } })
export const float = system({ float: { property: "float" } })
export const clear = system({ clear: { property: "clear" } })
export const cursor = system({ cursor: { property: "cursor" } })
export const wordWrap = system({ wordWrap: { property: "wordWrap" } })
